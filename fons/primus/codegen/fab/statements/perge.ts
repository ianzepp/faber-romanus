/**
 * Faber Code Generator - PergeStatement (continue)
 */

import type { FabGenerator } from '../generator';

export function genPergeStatement(g: FabGenerator): string {
    return `${g.ind()}perge`;
}
