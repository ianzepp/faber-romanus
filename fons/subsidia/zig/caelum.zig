// caelum.zig - HTTP helper library for Zig target (stub)
//
// NOT YET IMPLEMENTED - requires std.http client/server

const std = @import("std");

pub fn pete(url: []const u8) noreturn {
    _ = url;
    @compileError("caelum.pete not yet implemented for Zig target");
}

pub fn mitte(url: []const u8, corpus: []const u8) noreturn {
    _ = url;
    _ = corpus;
    @compileError("caelum.mitte not yet implemented for Zig target");
}

pub fn pone(url: []const u8, corpus: []const u8) noreturn {
    _ = url;
    _ = corpus;
    @compileError("caelum.pone not yet implemented for Zig target");
}

pub fn dele(url: []const u8) noreturn {
    _ = url;
    @compileError("caelum.dele not yet implemented for Zig target");
}

pub fn muta(url: []const u8, corpus: []const u8) noreturn {
    _ = url;
    _ = corpus;
    @compileError("caelum.muta not yet implemented for Zig target");
}

pub fn roga(
    modus: []const u8,
    url: []const u8,
    capita: anytype,
    corpus: []const u8,
) noreturn {
    _ = modus;
    _ = url;
    _ = capita;
    _ = corpus;
    @compileError("caelum.roga not yet implemented for Zig target");
}

pub fn exspecta(handler: anytype, portus: i64) noreturn {
    _ = handler;
    _ = portus;
    @compileError("caelum.exspecta not yet implemented for Zig target");
}

pub fn replicatio(status: i64, capita: anytype, corpus: []const u8) noreturn {
    _ = status;
    _ = capita;
    _ = corpus;
    @compileError("caelum.replicatio not yet implemented for Zig target");
}
