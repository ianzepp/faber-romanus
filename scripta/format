#!/usr/bin/env bash
# Format TypeScript source code
set -euo pipefail

cd "$(dirname "$0")/.."

if [[ "${1:-}" == "--check" ]]; then
    prettier --check "fons/**/*.ts"
else
    prettier --write "fons/**/*.ts"
fi
